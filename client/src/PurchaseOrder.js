import React from "react"
import PurchaseOrderCard from "./PurchaseOrderCard"

function PurchaseOrders({user, purchaseOrders}) {

    // const purchaseOrderCards = purchaseOrders.map((order) => (
    //     <PurchaseOrderCard
    //         key={order.id}
    //         orderNotes={order.order_notes}
    //         // items={order.items}
    //     />

    // ))
    // if (user !== null ){

    //     return <div id="item-collection">{purchaseOrderCards}</div>
    // } else {
    //     return (
    //     <h1>Uh oh, it looks like you've been logged out! Please log back in</h1>
    //     )
    // }
}

export default PurchaseOrders;